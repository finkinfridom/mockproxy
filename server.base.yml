version: '2.1'

services:
  base:
    environment:
      NODE_ENV: ${NODE_ENV}
      DEBUG: ${DEBUG}
      SERVER_PORT: ${SERVER_PORT}
      DB_HOST: ${DB_HOST}
      DB_DATABASE: ${DB_DATABASE}
      DB_USER: ${DB_USER}
      DB_PWD: ${DB_PWD}
